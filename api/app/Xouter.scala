import gg.duel.query.LookupDirection
import play.api.mvc._
import play.api.routing._
import play.api.routing.sird._

object Xouter {
//  val ld = new PathBindableExtractor[LookupDirection]
//  val router = Router.from {
//    case GET(p"/items/${ld(ldir)}") => Action {
//      Results.Ok(s"Item $ldir")
//    }
//  }

}
