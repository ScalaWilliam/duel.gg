akka {
    loglevel = "WARNING"
    actor {
        debug {
            receive = on
            autoreceive = on
            lifecycle = on
        }
    }
    loggers = ["akka.testkit.TestEventListener"]
}

