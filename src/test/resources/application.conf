akka {
    loglevel = "DEBUG"
    actor {
        debug {
            receive = on
            autoreceive = on
            lifecycle = on
        }
    }
    loggers = ["akka.testkit.TestEventListener"]
}

